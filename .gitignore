# ----------------------------------------------------------------------------
# ChatGPT Data Export Parser — .gitignore
#
# Purpose:
# - Never commit personal ChatGPT data
# - Keep generated output out of version control
# - Avoid committing Python runtime artifacts
# ----------------------------------------------------------------------------

# ----------------------------------------------------------------------------
# ChatGPT export files
#
# These contain private conversation data downloaded from OpenAI.
# They should NEVER be committed to a public or shared repository.
# ----------------------------------------------------------------------------

*.json
*.zip

# ----------------------------------------------------------------------------
# Sanitized example input data
#
# The examples/ directory contains intentionally fake, hand-crafted JSON files
# used for testing and demonstration purposes only. These files are sanitized
# and contain no personal data and are safe to commit in a public repository.
# ----------------------------------------------------------------------------

# Allow committed example input JSON files
!examples/*.json

# ----------------------------------------------------------------------------
# Rendered output (ignored)
#
# All rendered outputs (HTML, Markdown, plain text, etc.) are intentionally
# excluded from version control.
#
# During development, these files are transient artifacts used for local
# inspection while iterating on renderers. In production use, rendered outputs
# are fully reproducible from the input data and are expected to be generated
# by users, not committed to the repository.
# ----------------------------------------------------------------------------

# Rendered example / test outputs
examples/test-files/

# Generated output folders
out/
readable/

# ----------------------------------------------------------------------------
# Python runtime / cache 
#
# These files are automatically generated by Python during execution. They are
# environment-specific, frequently changing, and not relevant to the source.
# ----------------------------------------------------------------------------

__pycache__/
*.pyc

# ----------------------------------------------------------------------------
# Virtual environments
#
# Local Python environments should not be committed because they are platform-
# specific and can be recreated from requirements.
# ----------------------------------------------------------------------------

.venv/

# ----------------------------------------------------------------------------
# Local development / scratch workspace
#
# The dev/ directory is used for personal experimentation, notes, prototypes,
# and throwaway test scripts while iterating on the project.
#
# Contents of this folder are:
# - Not part of the supported codebase
# - Not intended for review or collaboration
# - Potentially messy, incomplete, or exploratory
#
# This folder is intentionally excluded from version control.
# ----------------------------------------------------------------------------

dev/
