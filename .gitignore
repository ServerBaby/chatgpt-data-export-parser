# ----------------------------------------------------------------------------
# ChatGPT Data Export Parser — .gitignore
#
# Purpose:
# - Never commit personal ChatGPT data
# - Keep generated output out of version control
# - Avoid committing Python runtime artifacts
# ----------------------------------------------------------------------------

# ----------------------------------------------------------------------------
# ChatGPT export files
# These contain private conversation data downloaded from OpenAI.
# They should NEVER be committed to a public or shared repository.
# ----------------------------------------------------------------------------
*.json
*.zip

# ----------------------------------------------------------------------------
# Explicit exception for sanitized example data
# The examples/ directory contains intentionally fake, hand-crafted JSON files
# used for testing and demonstration purposes only. These files are sanitized 
# and contain no personal data and are safe to commit in a public repository.
# ----------------------------------------------------------------------------
!examples/*.json

# ----------------------------------------------------------------------------
# Generated output
# These folders are created by the tool when rendering conversations into
# human-readable formats. They are fully reproducible from the input data and
# should not be versioned.
# ----------------------------------------------------------------------------
out/
readable/

# ----------------------------------------------------------------------------
# Python runtime / cache files
# These files are automatically generated by Python during execution. They are
# environment-specific, frequently changing, and not relevant to the source.
# ----------------------------------------------------------------------------
__pycache__/
*.pyc

# ----------------------------------------------------------------------------
# Virtual environments
# Local Python environments should not be committed because they are platform-
# specific and can be recreated from requirements.
# ----------------------------------------------------------------------------
.venv/
